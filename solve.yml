---
- name: Solving test environment
  hosts: all
  become: False
  gather_facts: False
  vars_files:
  - vars.yml
  tasks:
  - name: "Creating a file {{ filename }}"
    file: 
      path: "{{ filename }}"
      state: touch
